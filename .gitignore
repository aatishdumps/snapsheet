# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
dev-dist
*.local

# Lockfile is intentionally not committed: a Windows-generated npm
# package-lock.json fails Cloudflare's Linux `npm ci`. With no lockfile,
# Cloudflare runs `npm install`, which resolves cleanly on its own platform.
package-lock.json

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
